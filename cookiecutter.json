{
  "project_name": "fluxional_app",
  "use_poetry": "false",
  "use_github_actions": "true",
  "add_tests": "true",
  "_copy_without_render": [
    "{% if cookiecutter.use_github_actions == 'true' -%}deploy.yml{%- endif %}"
  ]
}
